name: Get latest release version
on:
  schedule:
    - cron: 0 10 * * *
  workflow_dispatch:
jobs:
  get-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Fetch release version
        run: npm info three version
